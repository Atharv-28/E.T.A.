import React from 'react';
import NewTransactionDetectedModal from '../modules/transactions/components/NewTransactionDetectedModal';

export default function TransactionCategoryModal(props) {
  return <NewTransactionDetectedModal {...props} />;
}
